[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-forge-trainer"
version = "0.1.0"
description = "Forge trainer — GPU training job manager with LoRA/embedding/genome modes"
license = {text = "MIT"}
requires-python = ">=3.8"
